import React from 'react';

const NotFoundPage = () => (
  <div id="not-found">
    <h3>Hmm... we can't find that page</h3>
    <h4>Please try a valid contact</h4>
  </div>
);

export default NotFoundPage;
